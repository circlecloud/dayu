export declare namespace query {
    export interface FilterOpt {
        filters?: string;
    }
    export interface LabelOpt {
        [key: string]: {
            [key: string]: boolean
        }
    }
}